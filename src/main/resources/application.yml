spring:
  profiles:
    active: local

springdoc:
  packagesToScan: com.example.sdui
  swagger-ui:
    path: /api-docs
    groups:
      enabled: false

logging:
  level:
    org.hibernate:
      SQL: debug
      type: trace
